
compile-job:
  stage: build
  script:
    - bundle install
    - rails -B

test-job:
  stage: test
  script:
    - bundle install
    - rails test

deploy-heroku-job:
  stage: deploy
  script:
    - dpl --provider=heroku --app=limitless-temple-80750 --api-key=$heroku_key
